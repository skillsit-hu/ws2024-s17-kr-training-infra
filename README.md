# README

for first time:

```
docker-compose build
docker-compose up
```

if u want to reset the database:

```
docker-compose down
docker-compose up
```


phpmyadmin:

```
http://localhost:8080
```

API: 

```
http://localhost:7078
```


